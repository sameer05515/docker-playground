export default function TopicModal({topic,onClose,onStatus,onNote}){if(!topic)return null;return <div className="backdrop" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><div className="modal"><button className="close" onClick={onClose}>×</button><span className="label">{topic.phase}</span><h2>{topic.title}</h2><div className="detail-row"><span className={'difficulty '+topic.difficulty}>{topic.difficulty}</span><span className="mins">{topic.minutes} min</span></div><p>{topic.summary}</p><h3>Interview Questions</h3><ul>{topic.questions?.map(q=><li key={q}>{q}</li>)}</ul><h3>My Notes</h3><textarea value={topic.note||''} onChange={e=>onNote(topic.topicId,e.target.value)} placeholder="Write your notes..."/><div className="modal-actions"><button onClick={()=>onStatus(topic.topicId)}>Change status</button><button className="primary" onClick={onClose}>Close</button></div></div></div>}
