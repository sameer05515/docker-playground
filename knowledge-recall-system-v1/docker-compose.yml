services:
  mongodb:
    image: mongo:8.0
    container_name: knowledge-recall-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: knowledge_db
    volumes:
      - knowledge_mongodb_data:/data/db

volumes:
  knowledge_mongodb_data:
